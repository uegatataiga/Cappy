<div class="genre_search">

    <div class="form-group">
    <%= form_with url: genresearches_genre_search_path, method: :get do |f| %>
    &emsp;<%= f.label :ジャンル検索 %></br>
    &emsp;<%= f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :name) %>
    <%= f.submit "検索", class: 'btn btn-info btn-sm' %>
    <% end %>
    </div>

</div>


